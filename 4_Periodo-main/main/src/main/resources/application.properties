# configuração simples (sem variáveis ${...})

# opção 1: H2 em memória (padrão — sobe sem depender do MySQL)
#spring.datasource.url=jdbc:h2:mem:app_db;DB_CLOSE_DELAY=-1;MODE=MySQL
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=

# jpa / hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# opção 2: MySQL local (recomendado pra validar de verdade)
spring.datasource.url=jdbc:mysql://localhost:3306/app_db?createDatabaseIfNotExist=true&serverTimezone=UTC&useSSL=false
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=senha

# server.port=8080
